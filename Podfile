# Uncomment the next line to define a global platform for your project
 platform :ios, '12.2'

target 'TriviaProject' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for TriviaProject

  pod 'Firebase/Core'
  pod 'Firebase/Database'
  pod 'Firebase/Auth'

  target 'TriviaProjectTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'TriviaProjectUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
